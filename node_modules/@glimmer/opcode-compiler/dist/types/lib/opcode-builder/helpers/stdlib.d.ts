import { StdLib } from '../stdlib';
import { CompileTimeCompilationContext } from '@glimmer/interfaces';
import { PushStatementOp } from '../../syntax/compilers';
export declare function main(op: PushStatementOp): void;
/**
 * Append content to the DOM. This standard function triages content and does the
 * right thing based upon whether it's a string, safe string, component, fragment
 * or node.
 *
 * @param trusting whether to interpolate a string as raw HTML (corresponds to
 * triple curlies)
 */
export declare function StdAppend(op: PushStatementOp, trusting: boolean, nonDynamicAppend: number | null): void;
export declare function compileStd(context: CompileTimeCompilationContext): StdLib;
//# sourceMappingURL=stdlib.d.ts.map